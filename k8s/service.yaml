apiVersion: v1
kind: Service
metadata:
  name: tictactoe-service
  labels:
    app: tictactoe-app
spec:
  type: ClusterIP
  selector:
    app: tictactoe-app
  ports:
    - port: 80
      targetPort: 80
